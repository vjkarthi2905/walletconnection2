{
  "name": "telegram-wallet-connect",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist && cp index.html dist/ && cp -r node_modules dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "@web3modal/wagmi": "^3.0.0",
    "viem": "^2.0.0",
    "wagmi": "^2.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
